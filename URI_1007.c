// CODE FOR: Difference.

#include <stdio.h>

int main()
{
    // Taking input of 4 integer
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);

    // Calculation for difference and printing the output
    printf("DIFERENCA = %d\n", a * b - c * d);

    return 0;
}